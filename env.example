# Cloudflare Tunnel Token (lấy từ Cloudflare dashboard)
# 1. Vào Zero Trust > Access > Tunnels
# 2. Click "Create a tunnel"
# 3. Chọn "Cloudflared" và copy token
CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token_here

# N8N Host URL (sau khi thiết lập tunnel, tab Public hostnames)
# Ví dụ: https://n8n.yourdomain.com
N8N_HOST=https://your-subdomain.your-domain.com